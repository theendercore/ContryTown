package org.teamvoided.civilization.util

import net.minecraft.server.network.ServerPlayerEntity
import java.util.UUID

object PlayerHelper {

    fun getPlayer(id:UUID): ServerPlayerEntity {
        return null as ServerPlayerEntity
    }
}